name: CI

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  typecheck:
    name: Type Check
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Type check
        run: bash scripts/ci-typecheck

  build-cli-binary:
    name: Build CLI Binary
    runs-on: ubuntu-latest
    needs: typecheck

    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Build CLI binary
        run: bash scripts/ci-build-cli-binary

      - name: Upload binary as artifact
        uses: actions/upload-artifact@v4
        with:
          name: subs
          path: packages/cli/subs

  build-api-docker:
    name: Build API Docker Image
    runs-on: ubuntu-latest
    needs: typecheck
    if: github.event_name == 'push'

    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v4

      - name: Get short SHA
        id: sha
        run: echo "short=$(echo ${{ github.sha }} | cut -c1-7)" >> $GITHUB_OUTPUT

      - name: Build the Docker image
        run: |
          IMAGE=ghcr.io/${{ github.repository_owner }}/${{ github.event.repository.name }}-api
          docker build . --file packages/api/Dockerfile \
            --tag $IMAGE:latest \
            --tag $IMAGE:${{ steps.sha.outputs.short }}

      - name: Log in to GitHub Container Registry
        run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

      - name: Push the Docker image
        run: |
          IMAGE=ghcr.io/${{ github.repository_owner }}/${{ github.event.repository.name }}-api
          docker push $IMAGE:latest
          docker push $IMAGE:${{ steps.sha.outputs.short }}

      - uses: actions/delete-package-versions@v5
        with:
          package-name: "${{ github.event.repository.name }}-api"
          package-type: "container"
          delete-only-untagged-versions: "true"

      - uses: actions/delete-package-versions@v5
        with:
          package-name: "${{ github.event.repository.name }}-api"
          package-type: "container"
          min-versions-to-keep: 1
          ignore-versions: "^latest$"

  build-web-docker:
    name: Build Web Docker Image
    runs-on: ubuntu-latest
    needs: typecheck
    if: github.event_name == 'push'

    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v4

      - name: Get short SHA
        id: sha
        run: echo "short=$(echo ${{ github.sha }} | cut -c1-7)" >> $GITHUB_OUTPUT

      - name: Build the Docker image
        run: |
          IMAGE=ghcr.io/${{ github.repository_owner }}/${{ github.event.repository.name }}-web
          docker build . --file packages/web/Dockerfile \
            --tag $IMAGE:latest \
            --tag $IMAGE:${{ steps.sha.outputs.short }}

      - name: Log in to GitHub Container Registry
        run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

      - name: Push the Docker image
        run: |
          IMAGE=ghcr.io/${{ github.repository_owner }}/${{ github.event.repository.name }}-web
          docker push $IMAGE:latest
          docker push $IMAGE:${{ steps.sha.outputs.short }}

      - uses: actions/delete-package-versions@v5
        with:
          package-name: "${{ github.event.repository.name }}-web"
          package-type: "container"
          delete-only-untagged-versions: "true"

      - uses: actions/delete-package-versions@v5
        with:
          package-name: "${{ github.event.repository.name }}-web"
          package-type: "container"
          min-versions-to-keep: 1
          ignore-versions: "^latest$"
